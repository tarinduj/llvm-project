// RUN: mlir-opt -canonicalize %s | FileCheck %s

// CHECK-LABEL: func @performance3863
func @performance3863() -> i1 {
  // CHECK-NEXT: %[[S:.*]] = constant true
  // CHECK-NEXT: return %[[S]]
  %set = presburger.set #presburger<"(d0, d1, d2, d3)[] : (d3  = 0 and d2 + -496 = 0 and d0 + -1 >= 0 and -d0 + 999 >= 0 and d1 + -465 >= 0 and -d1 + 477 >= 0  or d3  = 0 and d2 + -32 = 0 and d0 + -1 >= 0 and -d0 + 999 >= 0 and d1 + -1 >= 0 and -d1 + 13 >= 0 )">

  %r = presburger.coalesce %set : !presburger.set<4,0>
  %e = presburger.equal %set, %r : !presburger.set<4,0>, !presburger.set<4,0>
  return %e : i1
}

// ----
