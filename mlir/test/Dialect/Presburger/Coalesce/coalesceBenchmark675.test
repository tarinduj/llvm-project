// RUN: mlir-opt -canonicalize %s | FileCheck %s

// CHECK-LABEL: func @performance675
func @performance675() -> i1 {
  // CHECK-NEXT: %[[S:.*]] = constant true
  // CHECK-NEXT: return %[[S]]
  %set = presburger.set #presburger<"(d0, d1, d2, d3, d4, d5)[] : (-d2 + 16d5  = 0 and -d1 + d4  = 0 and d3 + -1 = 0 and d0  = 0 and d1  >= 0 and -d1 + d2 + -2 >= 0 and -d2 + 496 >= 0  or -d1 + d4  = 0 and d3 + -1 = 0 and -d1 + d2 + -1 = 0 and d0  = 0 and d1  >= 0 and -d1 + 498 >= 0 and -d1 + 16d5 + 14 >= 0 and d1 + -16d5 + 1 >= 0 )">

  %r = presburger.coalesce %set : !presburger.set<6,0>
  %e = presburger.equal %set, %r : !presburger.set<6,0>, !presburger.set<6,0>
  return %e : i1
}

// ----
