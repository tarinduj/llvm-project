// RUN: mlir-opt -canonicalize %s | FileCheck %s

// CHECK-LABEL: func @performance9245
func @performance9245() -> i1 {
  // CHECK-NEXT: %[[S:.*]] = constant true
  // CHECK-NEXT: return %[[S]]
  %set = presburger.set #presburger<"(d0, d1, d2, d3)[] : (-d0 + -2 >= 0 and d0 + d1 + -3 >= 0 and d0 + -d1 + 9 >= 0  or -d0 + -2 >= 0 and d0 + d1 + 3 >= 0 and d0 + -d1 + 3 >= 0  or d0 + 1 >= 0 and -d0 + d1 + -6 >= 0 and -d0 + -d1 + 6 >= 0  or d0 + 1 >= 0 and -d0 + d1  >= 0 and -d0 + -d1  >= 0 )">

  %r = presburger.coalesce %set : !presburger.set<4,0>
  %e = presburger.equal %set, %r : !presburger.set<4,0>, !presburger.set<4,0>
  return %e : i1
}

// ----
