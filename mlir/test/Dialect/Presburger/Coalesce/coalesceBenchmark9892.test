// RUN: mlir-opt -canonicalize %s | FileCheck %s

// CHECK-LABEL: func @performance9892
func @performance9892() -> i1 {
  // CHECK-NEXT: %[[S:.*]] = constant true
  // CHECK-NEXT: return %[[S]]
  %set = presburger.set #presburger<"(d0, d1)[] : (-d0 + d1  + 9 >= 0 and d0 + d1  + -3 >= 0 and -d0 + -d1  + 6 >= 0 and d0 + -d1  + -6 >= 0  or -d0 + d1  + 3 >= 0 and d0 + d1  + 3 >= 0 and -d0 + -d1  >= 0 and d0 + -d1  >= 0 )">

  %r = presburger.coalesce %set : !presburger.set<2,0>
  %e = presburger.equal %set, %r : !presburger.set<2,0>, !presburger.set<2,0>
  return %e : i1
}

// ----
